"use client";
import { useState } from "react";
export default function ForgotPasswordForm(){const[email,setEmail]=useState("");const[sent,setSent]=useState(false);return sent?<div className="mt-8 rounded-2xl bg-sand p-5 text-sm">If an account exists for that email, a reset link has been sent.</div>:<form className="mt-8 space-y-4" onSubmit={async e=>{e.preventDefault();await fetch("/api/auth/forgot-password",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email})});setSent(true)}}><input required type="email" autoComplete="email" placeholder="Email address" value={email} onChange={e=>setEmail(e.target.value)} className="w-full rounded-xl border bg-transparent p-4"/><button className="w-full rounded-full bg-charcoal py-3 text-white">Send reset link</button></form>}