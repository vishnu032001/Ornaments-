"use client";
import Link from "next/link";
import { useState } from "react";
export default function ResetPasswordForm({token}:{token:string}){const[password,setPassword]=useState("");const[done,setDone]=useState(false);const[error,setError]=useState("");if(done)return <div className="mt-8 rounded-2xl bg-sand p-5">Password updated. <Link href="/login" className="underline">Sign in</Link></div>;return <form className="mt-8 space-y-4" onSubmit={async e=>{e.preventDefault();setError("");const r=await fetch("/api/auth/reset-password",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({token,password})});const data=await r.json();if(!r.ok){setError(data.error||"Unable to reset password.");return}setDone(true)}}><input required minLength={8} maxLength={72} type="password" autoComplete="new-password" placeholder="New password (8+ characters)" value={password} onChange={e=>setPassword(e.target.value)} className="w-full rounded-xl border bg-transparent p-4"/>{error&&<p role="alert" className="text-sm text-red-700">{error}</p>}<button disabled={!token} className="w-full rounded-full bg-charcoal py-3 text-white disabled:opacity-50">Update password</button></form>}