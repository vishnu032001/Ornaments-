"use client"; import {useState} from "react"; import Link from "next/link"; import {useStore} from "@/lib/store"; import {formatINR,shippingFor,taxFor} from "@/lib/utils";
export default function Checkout(){const{items,clearCart}=useStore();const[placed,setPlaced]=useState(false);const subtotal=items.reduce((s,i)=>s+i.product.price*i.quantity,0),shipping=shippingFor(subtotal),tax=taxFor(subtotal),total=subtotal+shipping+tax;if(placed)return <section className="mx-auto max-w-2xl px-6 py-24 text-center"><div className="mx-auto grid h-16 w-16 place-items-center rounded-full bg-sand text-2xl">✓</div><h1 className="mt-6 font-serif text-5xl">Order received</h1><p className="mt-4 text-stone-600">This is a demo checkout. Connect payment and order APIs before accepting live orders.</p><Link href="/shop" className="mt-8 inline-flex rounded-full bg-charcoal px-7 py-3 text-white">Continue shopping</Link></section>;if(!items.length)return <section className="mx-auto max-w-xl px-6 py-24 text-center"><h1 className="font-serif text-5xl">Your bag is empty</h1><Link href="/shop" className="mt-8 inline-flex rounded-full bg-charcoal px-7 py-3 text-white">Shop jewellery</Link></section>;return <section className="mx-auto max-w-6xl px-6 py-12"><h1 className="font-serif text-5xl">Checkout</h1><div className="mt-10 grid gap-12 lg:grid-cols-[1fr_380px]"><form onSubmit={e=>{e.preventDefault();setPlaced(true);clearCart()}} className="space-y-8"><fieldset><legend className="font-serif text-2xl">Contact</legend><input required type="email" placeholder="Email address" className="mt-4 w-full rounded-xl border bg-transparent p-4"/></fieldset><fieldset><legend className="font-serif text-2xl">Delivery</legend><div className="mt-4 grid gap-3 md:grid-cols-2"><input required placeholder="Full name" className="rounded-xl border bg-transparent p-4 md:col-span-2"/><input required placeholder="Address" className="rounded-xl border bg-transparent p-4 md:col-span-2"/><input required placeholder="City" className="rounded-xl border bg-transparent p-4"/><input required placeholder="Postal code" className="rounded-xl border bg-transparent p-4"/></div></fieldset><fieldset><legend className="font-serif text-2xl">Payment</legend><div className="mt-4 rounded-xl border p-4 text-sm text-stone-500">Demo payment form. No real payment is processed.</div></fieldset><button className="w-full rounded-full bg-charcoal py-4 text-white">Place demo order · {formatINR(total)}</button></form><aside className="h-fit rounded-2xl bg-sand p-6"><h2 className="font-serif text-2xl">Order summary</h2>{items.map(i=><div key={i.product.id} className="mt-4 flex justify-between gap-4 text-sm"><span>{i.product.name} × {i.quantity}</span><span>{formatINR(i.product.price*i.quantity)}</span></div>)}<div className="mt-6 border-t pt-4 text-sm"><div className="flex justify-between"><span>Subtotal</span><span>{formatINR(subtotal)}</span></div><div className="mt-2 flex justify-between"><span>Shipping</span><span>{shipping?formatINR(shipping):"Free"}</span></div><div className="mt-2 flex justify-between"><span>Tax</span><span>{formatINR(tax)}</span></div><div className="mt-4 flex justify-between text-lg font-medium"><span>Total</span><span>{formatINR(total)}</span></div></div></aside></div></section>}